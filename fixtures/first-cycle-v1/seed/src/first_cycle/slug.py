def slugify(text: str) -> str:
    """Return a URL-safe slug for ``text``.

    Intentionally unimplemented: the orchestrator's agent task is to author a
    test under tests/ and then make it pass, without expanding project scope.
    """
    raise NotImplementedError("implement me")
