# happy-path-v1 seed repo

Disposable target for orchestrator walkthroughs. Agents implement `greet`;
tests already define success.

```bash
python -m pytest -q
```

Do not use this tree as a real product. Reset to tag `happy-path-v1-seed` between runs.
