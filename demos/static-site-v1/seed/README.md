# sitegen

A tiny static site generator. Reads markdown files with a front-matter header
and writes a small HTML site.

Nothing is implemented yet — see `content/` for the input this must handle.

    sitegen build content/ --out out/
