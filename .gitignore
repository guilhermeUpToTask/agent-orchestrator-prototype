# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
# NOTE: keep the Python build-artifact patterns repo-anchored (leading slash).
# A blanket `lib/` here matched `frontend/src/lib/` at any depth and silently
# excluded the entire frontend data layer from git — see `!frontend/src/lib/`.
.Python
/build/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg
MANIFEST

# Belt-and-suspenders: never ignore the frontend data layer.
!frontend/src/lib/

# Virtual environments (uv default)
.venv/
venv/
env/
ENV/

# uv

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
pytest_cache/
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache

# Type checkers
.mypy_cache/
.pyre/
.pytype/

# Linters
.ruff_cache/

# Jupyter
.ipynb_checkpoints

# Environments
.env
.env.*
*.env
!.env.example

# Logs
*.log

# Local SQLite runtime snapshots
*.db

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

#workflow running aplication
workflow/

node_modules/

# Ignore Graphify local
.graphify/
graphify-out/

# Local Claude Code settings (per-user, not shared)
.claude/settings.local.json
frontend/.claude/settings.local.json

# Accelerate: transient git worktree scratch (worktrees are created at runtime)
.orchestrator/worktrees/