task_id: task-3c9b
feature_id: feat-auth
title: "Implement POST /login"
description: |
  Add login endpoint that validates credentials and returns JWT.
agent_selector:
  required_capability: "backend_dev"
  min_version: ">=1.0.0"
execution:
  type: "code:backend"
  constraints:
    language: "python"
    framework: "fastapi"
  test_command: "pytest tests/test_auth.py -q"
  acceptance_criteria:
    - "All tests pass"
    - "New endpoint follows API spec"
  files_allowed_to_modify:
    - "app/auth.py"
    - "tests/test_auth.py"
status: created
assignment: null
state_version: 1
history: []
result: null
retry_policy:
  max_retries: 2
  backoff_seconds: 30
  attempt: 0
