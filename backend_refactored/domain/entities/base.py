"""Reserved for a shared Entity base (typed id + identity equality). Not introduced
yet — the entities are small and share no behavior. See ../../DESIGN_NOTES.md
(generic Entity base) for the tradeoffs and a sketch."""
